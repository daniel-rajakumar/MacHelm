{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug MacHelm App",
            "program": "${workspaceFolder}/app/.build/arm64-apple-macosx/debug/MacHelm",
            "args": [],
            "cwd": "${workspaceFolder}/app",
            "preLaunchTask": "🏗️ MacHelm: Build App",
            "postDebugTask": "🛑 MacHelm: Stop App",
            "initCommands": [
                "process handle SIGTERM -n true -p true -s false"
            ]
        }
    ]
}
